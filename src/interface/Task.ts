export default interface Task {
    name: string;
    description: string;
    priorityLevel: string;
    estimatedTime: string;
    estimatedTimeUnit: string;
    status: string;
    deadline: string;
}